name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1

      - name: Bootstrap
        run: npm i

      - name: Test
        run: npm run test

      - name: Build Integrations
        run: npx lerna run build --scope=@lit-kit-demos/*

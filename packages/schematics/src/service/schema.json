{
  "$schema": "http://json-schema.org/schema",
  "id": "serviceSchema",
  "title": "generates a new Service",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The service name",
      "x-prompt": "What is the name of your service?",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": ["name"]
}

import { createComponent } from '@lit-kit/component';

import { <%= classify(name) %>Component, <%= classify(name) %>State } from './<%= name %>.component.ts';

describe('<%= classify(name) %>Component', () => {
  it('should work', () => {
    const el = createComponent<<%= classify(name) %>Component, <%= classify(name) %>State>(<%= classify(name) %>Component);

    expect(el).toBeTruthy();
  });
});

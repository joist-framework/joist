import { Component, StateRef, State } from '@joist/component';
import { html } from 'lit-html';

export interface <%= classify(name) %>State {}

@Component<<%= classify(name) %>State>({
  state: {},
  render({ state }) {
    return html`
      <%= name %> Worked!
    `;
  }
})
export class <%= classify(name) %>Component {
  constructor(@StateRef _state: State<<%= classify(name) %>State>) {}
}

customElements.define('<%= name %>', defineElement(<%= classify(name) %>Component));
